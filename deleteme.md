data-model.REQ_STATUSES.8 is newly added and has not been implemented

QA items - team-list.CREATE.2 is not acceptable, there is a bug where the modal dismisses itself if I click anywhere into it (including the field)

- Same bug exists for the modal in the create access token

TEAM_TOKENS.MAIN.4 and 4-1 were not implemented at all, from what I can see (modal dismisses itself) and
